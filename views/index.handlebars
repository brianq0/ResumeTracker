<h1>Resume</h1>

<h2>Resume's that are not liked!</h2>

<ul>
    {{#each resume}}
    {{#unless resLiked}}
    {{> resume-block liked=false}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Resume's that are liked!</h2>

<ul>
    {{#each resume}}
    {{#if resLiked}}
    {{> resume-block liked=true}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Resume</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Cat Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
        <label for="slee">liked?</label><br>
        <input type="radio" name="liked" value="1" checked> liked!<br>
        <input type="radio" name="liked" value="0"> Awake
    </div>

    <button type="submit">Add Cat</button>
</form>

<script>



</script>